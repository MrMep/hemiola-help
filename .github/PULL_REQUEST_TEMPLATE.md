## Description

Brief description of what this PR changes.

## Checklist

- [ ] I ran `python tools/validate_help.py` and all files pass
- [ ] Section structure (IDs, icons, entry count) is unchanged unless intentional
- [ ] Only `title` and `text` fields were modified (for translations/fixes)
- [ ] The `"language"` field matches the filename suffix

## Languages affected

List the files you changed (e.g. `help_fr.json`).
